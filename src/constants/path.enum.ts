export enum path {
  homepage = "/",
  login = "/login",
  register = "/register",
  profile = "/profile",
  updateProfile = "/profile/:userId",
  orders = "/orders",
  orderDetail = "/orders/:orderId",
  tracks = "/tracks",
  trackDetails = "/:trackId",
  payment = "/payment",
  dashboard = "/dashboard",
}
