export enum path {
  homepage = "/",
  login = "/login",
  register = "/register",
  profile = "/profile",
  updateProfile = "/profile/:userId",
  orders = "/orders",
  tracks = "/tracks",
  trackDetails = "/:trackId",
  payment = "/payment",
}
